// =====================================================================
// Este archivo es reemplazado automáticamente en cada build de Netlify
// por scripts/inject-env.sh, que sustituye estos valores por las
// variables de entorno reales configuradas en Netlify:
//   SUPABASE_URL, SUPABASE_ANON_KEY
//
// EN LOCAL: copia este archivo y reemplaza los valores manualmente
// para poder probar sin desplegar (no lo subas con tus claves reales
// a un repositorio público).
// =====================================================================
window.__ENV__ = {
  SUPABASE_URL: "__SUPABASE_URL__",
  SUPABASE_ANON_KEY: "__SUPABASE_ANON_KEY__",
};
